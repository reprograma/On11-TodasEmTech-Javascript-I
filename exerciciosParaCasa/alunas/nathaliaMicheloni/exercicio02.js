// Exercício para casa nº 2:

let myName = 'Default';    // Substituído o const por let, pois const não permite reatribuição
     myName = 'Chris';

     let myAge = 42; // Excluído as aspas simples para que o valor possa ser reconhecido como number ao invés de string

     console.log(myName);    
     console.log(`In 20 years, I will be ${myAge + 20}`); // Excluído uma "/" que estáva no console.log